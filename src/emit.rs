use super::structures::*;
use super::emit_structures::*;
use super::error::EmitError;

pub fn emit_module(root_module_info: ModuleInfo) -> Result<(JsonOutput, JsOutput), EmitError> {

    todo!();
}

fn emit_json(root_module_info: &ModuleInfo) -> Result<JsonOutput, EmitError> {
    todo!();
}
